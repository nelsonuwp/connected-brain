# =========================================================
# === DAILY DIGEST (add to your root .env) ===
# =========================================================

# --- User Identity ---
# type: string | required — your primary email (used for Graph API + is_from_me detection)
DIGEST_USER_EMAIL=adam.nelson@aptum.com

# type: string | required — comma-separated display names (for Teams/Slack identity matching)
DIGEST_USER_DISPLAY_NAMES=Adam Nelson,Adam

# --- LLM ---
# type: string | optional | default: anthropic/claude-sonnet-4-5
# Model used for triage in summarize.py (via OpenRouter)
DIGEST_LLM_MODEL=anthropic/claude-sonnet-4-5

# --- Embedding ---
# type: string | optional | default: all-MiniLM-L6-v2
DIGEST_EMBEDDING_MODEL=all-MiniLM-L6-v2

# type: float | optional | default: 0.72
# Cosine similarity threshold for cross-source item merging
DIGEST_MERGE_THRESHOLD=0.72

# type: boolean | optional | default: false
# Use TF-IDF fallback when sentence-transformers is unavailable
DIGEST_USE_FALLBACK_EMBEDDING=false

# --- Slack (when ready) ---
# type: string | optional | secret — Slack Bot Token (xoxb-...)
# Setup: https://api.slack.com/apps → Create App → Bot Token Scopes:
#   channels:history, channels:read, groups:history, groups:read,
#   im:history, im:read, mpim:history, mpim:read, users:read
SLACK_BOT_TOKEN=

# type: string | optional — your Slack user ID (for is_from_me detection)
# Find it: Slack → Profile → ⋮ → Copy member ID
SLACK_USER_ID=

# type: string | optional — Slack workspace ID (for logging)
SLACK_WORKSPACE_ID=

# --- Microsoft Graph (reuses existing vars) ---
# Already in your .env:
#   MS_CLIENT_ID, MS_TENANT_ID, MS_CLIENT_SECRET, MS_EMAIL, MS_REDIRECT_URI
#
# IMPORTANT: Update your Azure App Registration to add these API permissions:
#   - Chat.Read        (for Teams chat messages)
#   - Chat.ReadBasic   (for Teams chat list)
#   Your existing Mail.Read and User.Read permissions remain.
