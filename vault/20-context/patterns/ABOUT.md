# 20-context/patterns  ·  Reference → Patterns

Reusable approaches. How we do things here.

**Contains:**
- Code patterns (retry logic, pagination, error handling, logging)
- Process patterns (how to structure a post-mortem, how to run escalation)
- Prompt patterns (LLM prompt structures that reliably work)
- Architectural patterns (service communication, data flow conventions)

**Rules:**
- Every pattern has a "when to use / when NOT to use" section
- Inject into Cursor when the task matches a known pattern
- When a project solves a new problem well → extract the reusable pattern here
