# Connected Brain — Structure

## Repository Layout

```
~/connected-brain/
├── vault/                    ← Obsidian vault root
│   ├── _system/              ← this documentation
│   ├── _templates/           ← Obsidian note templates
│   ├── _prompts/             ← LLM prompt files (used by brain.py)
│   ├── _attachments/         ← images, PDFs, binary files
│   ├── 00-daily/             ← daily and weekly notes
│   ├── 01-inbox/             ← raw idea captures
│   ├── 10-thinking/          ← structured thinking notes
│   ├── 20-context/           ← reusable context blocks for LLM injection
│   ├── 30-initiatives/       ← initiative specs
│   │   ├── drafting/         ← specs being developed
│   │   ├── active/           ← in execution
│   │   └── done/             ← completed
│   ├── 40-people/            ← person notes and 1:1 logs
│   ├── 50-services/          ← internal service delivery units
│   ├── 51-catalog/           ← external products and offerings
│   ├── 52-customers/         ← customer accounts and relationships
│   ├── 60-decisions/         ← permanent decision log
│   ├── 70-delegation/        ← delegation briefs
│   ├── 80-sessions/          ← LLM session re-anchors
│   └── 90-meeting-notes/     ← meeting notes
└── projects/                 ← self-contained code projects
    ├── _reference/           ← pattern library (copy, never import)
    │   └── schemas/          ← source_artifact.py, llm_io.py
    └── llm-bridge/           ← brain.py runtime
```

---

## Folder Definitions

### `_system/`
This documentation. Five files only — README, structure, tooling, playbook,
CHANGELOG. Do not add notes here. Do not use this folder for anything else.

### `_templates/`
Obsidian note templates applied by Templater on file creation or via
`Cmd+Shift+E`. One file per note type. Do not put prompts here — prompts
live in `_prompts/`.

Current templates: `idea`, `thinking-note`, `initiative-spec`, `daily-briefing`,
`weekly-review`, `meeting-note`, `person-note`, `context-block`,
`delegation-brief`, `re-anchor`

### `_prompts/`
LLM prompt files consumed by brain.py. Named exactly as brain.py expects them.
Do not rename without updating brain.py. Manual-use prompts (not called by
brain.py) also live here but are documented as manual-use in `tooling.md`.

### `_attachments/`
Binary files — images, PDFs, spreadsheets. Obsidian links to these from notes.
Not committed to git if large.

### `00-daily/`
Daily briefing notes (format: `YYYY-MM-DD.md`) and weekly review notes
(format: `YYYY-[W]ww.md`). Generated by Periodic Notes plugin. Contains
the Dataview dashboard showing active initiatives.

### `01-inbox/`
Raw idea captures from QuickAdd (`Cmd+Shift+I`). Three-field format: title,
what is it, why now. These are the starting point of the workflow. Nothing
here is developed — it's just captured. Triage regularly.

**What belongs:** Freshly captured ideas only.
**What doesn't belong:** Anything with more than a paragraph of content
belongs in `10-thinking/` instead.

### `10-thinking/`
Structured thinking notes. Generated by `brain idea promote` from an idea
in `01-inbox/`. Each note has defined sections (Why This Matters, The
Approach, Options Considered, Assumptions & Risks). LLM explore and
critique sections accumulate below the main document as working history.

**What belongs:** One note per idea being actively developed.
**What doesn't belong:** Raw captures (→ `01-inbox/`), ready-to-execute
specs (→ `30-initiatives/drafting/`).

**Sub-folder:** `archive/` — originals moved here on promote. Do not edit
archived files.

### `20-context/`
Reusable context blocks injected into LLM sessions via `--context` flag.
Each file is a self-contained reference document on one topic. The goal is
to write these once and inject them instead of re-explaining.

**Sub-folders:** `apis/`, `business/`, `schemas/`, `people/`, `systems/`

**What belongs:** API references, data schemas, business model descriptions,
system architecture notes, anything you find yourself re-explaining to LLMs.
**What doesn't belong:** Notes in progress — context blocks should be
authoritative and relatively stable. If it changes weekly, it's not ready
to be a context block.

**Naming:** `kebab-case.md` — descriptive and specific.
Example: `jira-api-reference.md`, `osom-model.md`, `salesforce-schema.md`

### `30-initiatives/`
Initiative specs at three stages of maturity. See the workflow in
`playbook.md` for how notes move through these folders.

**`drafting/`** — Specs generated by `brain thinking promote`. Being
developed via explore/critique/fix loops. Not ready to execute.

**`active/`** — Specs moved here by `brain initiative promote`. Actively
being executed. Surfaced in daily note Dataview dashboard.

**`done/`** — Completed initiatives. Moved here manually when execution
is complete. Not surfaced in dashboard.

**What belongs:** One spec per initiative. The spec is the source of truth —
execution tracking lives in Jira.
**What doesn't belong:** Meeting notes, working docs, Cursor session logs —
those live in `80-sessions/` or `90-meeting-notes/`.

**Sub-folder per initiative:** `drafting/archive/` and `active/archive/`
for originals moved on promote.

### `40-people/`
One note per person you manage or work closely with. Contains role, delegation
level, capability map, and 1:1 log. Used by `one-on-one-prep.md` prompt.

**Naming:** `firstname-lastname.md`

### `50-services/`
Internal service delivery units — the things your team operates. Distinct
from catalog (what you sell) and customers (who you sell to).

### `51-catalog/`
External products and offerings sold to customers. Distinct from services
(how you deliver internally).

### `52-customers/`
Customer accounts and relationships. One note per account.

### `60-decisions/`
Permanent decision log. One note per significant decision. Linked from
initiative specs and thinking notes. The point is to never re-litigate a
decision that was already made — link to it instead.

**Naming:** `YYYY-MM-DD-short-description.md`

### `70-delegation/`
Delegation briefs. One per active delegation. Used to clarify ownership,
authority, and escalation criteria for work handed to others.

### `80-sessions/`
LLM session re-anchors. Organized by project sub-folder. Written at the end
of every Cursor or long LLM session so the next session can resume cold.

**Structure:** `80-sessions/{project-name}/session-001.md`

### `90-meeting-notes/`
Meeting notes. Generated by Templater folder mapping when a new note is
created in this folder.

**Naming:** `YYYY-MM-DD-meeting-name.md`

---

## Naming Conventions

| Thing | Format | Example |
|---|---|---|
| Daily note | `YYYY-MM-DD.md` | `2026-02-22.md` |
| Weekly note | `YYYY-[W]ww.md` | `2026-W08.md` |
| Idea / thinking / initiative | `YYYY-MM-DD-short-title.md` | `2026-02-22-llm-bridge.md` |
| Decision | `YYYY-MM-DD-short-description.md` | `2026-02-22-atomic-write-pattern.md` |
| Person note | `firstname-lastname.md` | `jane-smith.md` |
| Context block | `kebab-case-descriptive.md` | `jira-api-reference.md` |
| Session re-anchor | `session-NNN.md` inside project folder | `session-001.md` |
| Meeting note | `YYYY-MM-DD-meeting-name.md` | `2026-02-22-board-review.md` |

**Rules:**
- Always lowercase, always kebab-case
- Always date-prefix for anything that moves through the workflow
- No spaces, no underscores in filenames
- `.md` extension on everything in vault

---

## What Goes in Projects vs Vault

| If it's... | It lives in |
|---|---|
| A markdown note, thinking, context | `vault/` |
| Python code, scripts, config | `projects/` |
| A re-usable code pattern | `projects/_reference/` |
| A specific project's code | `projects/{project-name}/` |

Projects are standalone bubbles. They copy from `_reference/` — they never
import from it. Each project has its own `.env`, its own `requirements.txt`,
and owns its own copies of any shared patterns.
